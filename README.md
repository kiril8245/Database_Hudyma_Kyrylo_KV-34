
# **Розрахунково-графічна робота (РГР)**

## **Тема: Консольний застосунок для роботи з базою даних PostgreSQL (шаблон MVC)**

---

# **Опис проєкту**

Цей проєкт реалізує консольний застосунок на мові Python для взаємодії з базою даних PostgreSQL, створеною у pgAdmin 4.
Програма побудована за архітектурним шаблоном **MVC (Model-View-Controller)**, що забезпечує чітке розділення відповідальності між логікою роботи з даними, відображенням інформації та обробкою дій користувача.

Застосунок дозволяє виконувати всі основні операції з базою даних: перегляд таблиць, пошук, додавання нових записів, редагування існуючих, видалення, а також автоматичну генерацію тестових даних для всіх сутностей (Donor, Doctor, Donation, Registration, Blood_screen).
Реалізовано контроль типів даних та обробку помилок через `try-except`, щоб уникати некоректних транзакцій і скасованих операцій.

---

# **Структура проєкту**

```
RGR_Gudyma_KV-34
 ┣  main.py           # Точка входу в програму
 ┣  controllers.py    # Контролер — обробка дій користувача
 ┣  model.py          # Модель — SQL-запити, CRUD, генерація
 ┣  views.py          # Подання — консольний інтерфейс
 ┣  config.py         # Параметри підключення до PostgreSQL
 ┗  README.md         # Документація (за бажанням)
```

---

# **Архітектура MVC**

**Model (model.py)**
Містить усю логіку взаємодії з PostgreSQL: встановлення підключення, формування SQL-запитів, виконання CRUD-операцій, генерацію випадкових даних, контроль зв’язків 1:N, перевірку існування дочірніх записів та обробку транзакцій.

**View (views.py)**
Реалізує консольний інтерфейс. Забезпечує читабельне форматування даних за допомогою `tabulate`, виведення таблиць, результатів запитів і повідомлень для користувача.

**Controller (controllers.py)**
Отримує введення від користувача, викликає відповідні методи моделі, обробляє помилки та передає результати до View. Координує роботу всього застосунку.

**Config (config.py)**
Містить параметри підключення до PostgreSQL: ім’я БД, користувач, пароль, порт та хост.

**Main (main.py)**
Є точкою входу — запускає головний цикл роботи програми та ініціалізує контролер.

---

# **Фрагменти коду**

## **Фрагмент програмного внесення даних (model.py)**

```python
def insert(self, table: str, data: Dict[str, Any]) -> Tuple[bool, Optional[str]]:
    cols = []
    vals = []
    placeholders = []

    for k, v in data.items():
        cols.append(f'"{k}"')
        vals.append(v)
        placeholders.append("%s")

    if not cols:
        return False, "Немає даних для вставки."

    sql = f'INSERT INTO public."{table}" ({", ".join(cols)}) VALUES ({", ".join(placeholders)});'

    try:
        self.execute(sql, tuple(vals), fetch=False)
        return True, None
    except Exception as e:
        self.conn.rollback()
        return False, str(e)
```

---

## **Фрагмент програми редагування даних (model.py)**

```python
def update(self, table: str, pk: str, pk_val: Any,
           updates: Dict[str, Any]) -> Tuple[bool, Optional[str]]:
    if not updates:
        return True, None

    sets = []
    vals = []
    for k, v in updates.items():
        sets.append(f'"{k}" = %s')
        vals.append(v)
    vals.append(pk_val)

    sql = f'UPDATE public."{table}" SET {", ".join(sets)} WHERE "{pk}" = %s;'

    try:
        self.execute(sql, tuple(vals), fetch=False)
        return True, None
    except Exception as e:
        self.conn.rollback()
        return False, str(e)
```

---

## **Фрагмент програми видалення даних (model.py)**

```python
def delete(self, table: str, pk: str, pk_val: Any) -> Tuple[bool, Optional[str]]:
    if self.has_child_records(table, pk, pk_val):
        return False, "Не можна видалити: існують рядки у підлеглих таблицях."

    sql = f'DELETE FROM public."{table}" WHERE "{pk}" = %s;'
    try:
        self.execute(sql, (pk_val,), fetch=False)
        return True, None
    except Exception as e:
        self.conn.rollback()
        return False, str(e)
```

---

## **Фрагмент програми генерації випадкових даних (model.py)**

```python
def generate_donors(self, n: int):
    sql = """
    INSERT INTO public."Donor" ("Donor_Name", "Donor_Card")
    SELECT 
        'Donor_' || gs::text,
        'CARD-' || substr(md5(random()::text), 1, 8)
    FROM generate_series(1, %s) AS gs;
    """
    self.execute(sql, (n,), fetch=False)
```

(Аналогічні генерації реалізовано для Doctor, Donation, Registration, Blood_screen.)

---

# **Як запустити**

1. Встановити залежності:

```
pip install psycopg psycopg[binary] tabulate
```

2. Налаштувати `config.py` з параметрами підключення.

3. Запустити програму:

```
python main.py
```

---

# **Висновок**

У результаті виконання розрахунково-графічної роботи було створено повністю функціональний консольний застосунок для роботи з базою даних PostgreSQL. Програма реалізує всі необхідні можливості — перегляд, пошук, додавання, редагування, видалення та генерацію даних у таблицях, а також забезпечує контроль цілісності зв’язків 1:N і коректну обробку помилок транзакцій.

Застосування архітектури MVC дозволило створити структурований, гнучкий і легко розширюваний застосунок. Робота дала можливість закріпити практичні навички роботи з PostgreSQL, Python, організацією CRUD-операцій і структурою програмного забезпечення.

---

# **Автор**

Гудима Кирило, група КВ-34



#🩸 Система управління донорами крові
📘 Опис проєкту

Цей проєкт реалізує ER-модель предметної області «Система управління донорами крові».
Мета — автоматизувати процес обліку донорів, донацій, лікарів і скринінгів крові для підвищення ефективності та прозорості процесу донорства.

Система зберігає інформацію про:

донорів (ПІБ, картка донора);

проведені донації (дата, обсяг крові, відповідальний лікар);

медичних працівників;

результати аналізів крові.

🧩 Структура моделі
1. Донор (Donor)

Donor_ID (PK)

Donor_Name

Donor_Card

📄 Призначення: збереження основної інформації про донорів крові.

2. Лікар (Doctor)

Doctor_ID (PK)

Doctor_Name

Experience

📄 Призначення: облік медичних працівників, що проводять процедури забору крові.

3. Донація (Donation)

Donation_ID (PK)

Donation_Time

Blood_Amount

📄 Призначення: збереження даних про кожну процедуру здачі крові.

4. Скринінг крові (Blood_Screen)

Blood_Screen_ID (PK)

Result

Blood_Type

Donation_ID (FK)

📄 Призначення: облік результатів аналізу крові, пов’язаних із конкретною донацією.

5. Реєстрація (Registration)

Registration_ID (PK)

Doctor_ID (FK)

Donor_ID (FK)

Donation_ID (FK)

Date

📄 Призначення: реалізація зв’язків між донорами, лікарями та донаціями (через N:M).

🔗 Зв’язки між сутностями

Donor – Donation: зв’язок N:M (реалізується через таблицю Registration).

Doctor – Donation: зв’язок N:M (також реалізується через Registration).

Donation – Blood_Screen: зв’язок 1:N (одна донація — кілька аналізів).

⚙️ Функціональні залежності

Donor:
Donor_ID → {Donor_Name, Donor_Card}
Donor_Name → {Donor_Card}

Doctor:
Doctor_ID → {Doctor_Name, Experience}
Experience → {Doctor_Name}

Donation:
Donation_ID → {Donation_Time, Blood_Amount}
Donation_Time → {Blood_Amount}

Blood_Screen:
Blood_Screen_ID → {Result, Blood_Type}
Result → {Blood_Type}

Registration:
Registration_ID → {Doctor_ID, Donor_ID, Donation_ID, Date}

🧮 Нормалізація бази даних

База даних відповідає вимогам 1НФ, 2НФ і 3НФ, оскільки:

усі атрибути атомарні (1НФ);

усі неключові атрибути залежать від усього первинного ключа (2НФ);

відсутні транзитивні залежності між неключовими атрибутами (3НФ).

✅ Висновок

Розроблена база даних системи управління донорами крові дозволяє ефективно зберігати, обробляти та відстежувати інформацію про донорів, лікарів, донації та аналізи крові.
ER-діаграма побудована за нотацією Чена, структура бази є логічно узгодженою та готовою до реалізації в PostgreSQL
